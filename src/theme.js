const space = [6, 12, 16, 24, 30];
const fontSizes = [12, 14, 16, 18, 24, 32, 48, 64, 72]

const colors = {
  black: "#000",
  white: "#fff",
  dark: "#161616",
  book: "#CACACA"
};

export default {
  fonts: {
    body: "system-ui, sans-serif"
  },
  space,
  colors,
  fontSizes
};
